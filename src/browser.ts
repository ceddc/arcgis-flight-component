/**
 * Loads the ArcGIS scene custom element for standalone browser use, then
 * exposes this package's public flight API. Use this entry when the page needs
 * the scene element registered alongside the flight component.
 */
import "@arcgis/map-components/components/arcgis-scene";
export * from "./index";
